
name: kuiko_delivery
description: App KuikoDelivery para negocios en Santiago de Cuba.
version: 1.0.0
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^4.0.0
  geolocator: ^9.0.0
  latlong2: ^0.8.1

flutter:
  uses-material-design: true
  assets:
    - assets/icono.png
